package com.acorn.day2.문제풀이;

public class 기본형변수 {
	
	public static void main(String[] args) {
		
		// 기본형 타입
		// 기본형 변수 : 값 하나를 저장하는 변수(상자)
		int a; // byte, short, int, long
		char b;
		double c;
		boolean d;
		
		// 객체.멤버 (. =>멤버접근연산자) -> 멤버라는거는 덩어리가 있다는 것
		// 속성 + 매서드
		
		a=10;	// +- 21억 
		
//		a=null;
		
		//null -> 참조형변수가 기본값 : 참조하는 대상이 없다.
		String str=null;
		// 문자열 -> 객체 (속성 + 매서드) (char[] + 매서드)
		
		// Acorn acorn = null;
		// type : 크기 + 해석 
		//자료형(type) , acorn 이라는 참조형 변수는 참조하는 대상(주소)에 있는 내용을 Acorn type으로 해석하겠다
		// acorn = new Acorn();
		
	}

}
