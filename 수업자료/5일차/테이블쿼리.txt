-- 시퀀스
DROP SEQUENCE book_seq;
CREATE SEQUENCE book_seq START WITH 1 INCREMENT BY 1;

-- 테이블
DROP TABLE book PURGE;

CREATE TABLE book (
    id      NUMBER PRIMARY KEY,
    title   VARCHAR2(100),
    author  VARCHAR2(50),
    price   NUMBER
);

-- 테스트 데이터
INSERT INTO book VALUES (book_seq.NEXTVAL, '스프링 입문', '홍길동', 20000);
INSERT INTO book VALUES (book_seq.NEXTVAL, '자바의 정석', '남궁성', 30000);
COMMIT;



ALTER TABLE Book ADD cover VARCHAR2(200);
